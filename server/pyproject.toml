[project]
name = "aspls-server"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pygls==1.3.1",
    "lark==1.2.2",
]

[project.optional-dependencies]
dev = ["pytest==8.3.3"]

[tool.pytest.ini_options]
testpaths = ["tests"]
